%img{src: gravatar_icon(current_user.email, 120), class: 'img-polaroid'}
%h3
  Hi, #{current_user.name}

.profile-block
  %p
    %span.light Email:
    %strong= current_user.email

  %p
    %span.light GitLab profile:
    %strong
      = link_to current_user.username, (Api::Gitlab::SERVER_URL + '/u/' + current_user.username), target: "_blank"
